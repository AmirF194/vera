public fn f(@Unit -> @Byte)
  requires(true)
  ensures(true)
  effects(pure)
{
  handle[State<Byte>](@Byte = 0) {
    get(@Unit) -> { resume(@Byte.0) },
    put(@Byte) -> { resume(()) }
  } in {
    put(999);
    get(())
  }
}

public fn main(@Unit -> @Byte)
  requires(true)
  ensures(true)
  effects(pure)
{
  f(())
}

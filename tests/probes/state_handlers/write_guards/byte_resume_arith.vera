public fn f(@Unit -> @Int)
  requires(true)
  ensures(true)
  effects(pure)
{
  handle[State<Byte>](@Byte = 7) {
    get(@Unit) -> { resume(1 + 2) },
    put(@Byte) -> { resume(()) }
  } in {
    byte_to_int(get(()))
  }
}

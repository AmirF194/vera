public fn f(@Unit -> @Byte)
  requires(true)
  ensures(true)
  effects(pure)
{
  handle[State<Byte>](@Byte = 1) {
    get(@Unit) -> { resume(300) },
    put(@Byte) -> { resume(()) }
  } in {
    get(())
  }
}

public fn main(@Unit -> @Byte)
  requires(true)
  ensures(true)
  effects(pure)
{
  f(())
}

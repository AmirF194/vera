public fn go(@Int -> @Int) requires(true) ensures(true) effects(pure)
{
  handle[State<Byte>](@Byte = 5) {
    get(@Unit) -> { resume(9) },
    put(@Byte) -> { resume(()) }
  } in {
    byte_to_int(get(()))
  }
}

type Count = Nat;

public fn main(@Unit -> @Int)
  requires(true)
  ensures(true)
  effects(pure)
{
  handle[State<Count>](@Count = 10) {
    put(@Count) -> { resume(()) } with @Count = @Count.1 + @Count.0
  } in {
    put(5);
    nat_to_int(get(()))
  }
}

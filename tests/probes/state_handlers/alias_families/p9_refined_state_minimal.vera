-- P9: minimal State<refined-alias> - does it compile at all?
type Pos = { @Int | @Int.0 > 0 };

public fn main(@Unit -> @Int)
  requires(true)
  ensures(true)
  effects(pure)
{
  handle[State<Pos>](@Pos = 5) {
    get(@Unit) -> { resume(@Pos.0) }
  } in {
    get(())
  }
}

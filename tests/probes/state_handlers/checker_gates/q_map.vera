private fn p(@Map<String, Int> -> @Bool)
  requires(true)
  ensures(true)
  effects(pure)
{
  forall(@Int, @Map<String, Int>.0, fn(@Int -> @Bool) effects(pure) { true })
}
public fn main(@Unit -> @Int) requires(true) ensures(true) effects(pure) { 0 }

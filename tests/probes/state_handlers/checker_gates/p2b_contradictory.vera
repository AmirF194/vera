public fn t(@Unit -> @Int)
  requires(true)
  ensures(true)
  effects(pure)
{
  handle[State<{ @Nat | @Nat.0 < 10 }>](@{ @Nat | @Nat.0 > 999 } = 5) {
    put(@Nat) -> { resume(()) }
  } in {
    nat_to_int(get(()))
  }
}

private fn p(@Float64 -> @Bool)
  requires(true)
  ensures(true)
  effects(pure)
{
  forall(@Int, @Float64.0, fn(@Int -> @Bool) effects(pure) { true })
}
public fn main(@Unit -> @Int) requires(true) ensures(true) effects(pure) { 0 }

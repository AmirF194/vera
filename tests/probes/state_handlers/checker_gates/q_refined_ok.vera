type PosInt = { @Int | @Int.0 > 0 };
private fn p(@PosInt -> @Bool)
  requires(true)
  ensures(true)
  effects(pure)
{
  forall(@Int, @PosInt.0, fn(@Int -> @Bool) effects(pure) { true })
}
public fn main(@Unit -> @Int) requires(true) ensures(true) effects(pure) { 0 }

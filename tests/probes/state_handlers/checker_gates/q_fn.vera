private fn p(@Unit -> @Bool)
  requires(true)
  ensures(true)
  effects(pure)
{
  forall(@Int, fn(@Int -> @Int) effects(pure) { 1 }, fn(@Int -> @Bool) effects(pure) { true })
}
public fn main(@Unit -> @Int) requires(true) ensures(true) effects(pure) { 0 }

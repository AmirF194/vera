private fn p(@Byte -> @Bool)
  requires(true)
  ensures(true)
  effects(pure)
{
  forall(@Int, @Byte.0, fn(@Int -> @Bool) effects(pure) { true })
}
public fn main(@Unit -> @Int) requires(true) ensures(true) effects(pure) { 0 }

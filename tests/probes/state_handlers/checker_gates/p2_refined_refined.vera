public fn t(@Unit -> @Int)
  requires(true)
  ensures(true)
  effects(pure)
{
  handle[State<{ @Nat | @Nat.0 < 10 }>](@{ @Nat | @Nat.0 > 3 } = 5) {
    put(@Nat) -> { resume(()) }
  } in {
    put(1);
    nat_to_int(get(()))
  }
}

public fn main(@Unit -> @Int)
  requires(true)
  ensures(true)
  effects(pure)
{
  t(())
}

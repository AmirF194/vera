type Count = Nat;
public fn t(@Unit -> @Bool)
  requires(true)
  ensures(true)
  effects(pure)
{
  handle[State<Count>](@Count = 1) {
    get(@Unit) -> { resume(@Count.0) }
  } in {
    get(()) < 0
  }
}
public fn main(@Unit -> @Bool)
  requires(true)
  ensures(true)
  effects(pure)
{
  t(())
}

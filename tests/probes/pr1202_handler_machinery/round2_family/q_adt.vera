private fn p(@Option<Int> -> @Bool)
  requires(true)
  ensures(true)
  effects(pure)
{
  forall(@Int, @Option<Int>.0, fn(@Int -> @Bool) effects(pure) { true })
}
public fn main(@Unit -> @Int) requires(true) ensures(true) effects(pure) { 0 }

private fn p(@Int -> @Bool)
  requires(true)
  ensures(true)
  effects(pure)
{
  forall(@Int, no_such_fn(@Int.0), fn(@Int -> @Bool) effects(pure) { true })
}
public fn main(@Unit -> @Int) requires(true) ensures(true) effects(pure) { 0 }

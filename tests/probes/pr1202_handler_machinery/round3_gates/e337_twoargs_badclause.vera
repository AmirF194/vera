public fn f(@Unit -> @Int)
  requires(true)
  ensures(true)
  effects(pure)
{
  handle[State<Int, Nat>](@Int = 0) {
    get(@Unit) -> { resume(@Int.5 + @Bogus.0) },
    put(@Int) -> { resume(()) }
  } in {
    get(())
  }
}

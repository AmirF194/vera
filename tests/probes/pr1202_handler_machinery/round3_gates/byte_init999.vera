public fn f(@Unit -> @Byte)
  requires(true)
  ensures(true)
  effects(pure)
{
  handle[State<Byte>](@Byte = 999) {
    get(@Unit) -> { resume(@Byte.0) },
    put(@Byte) -> { resume(()) }
  } in {
    get(())
  }
}

public fn main(@Unit -> @Byte)
  requires(true)
  ensures(true)
  effects(pure)
{
  f(())
}

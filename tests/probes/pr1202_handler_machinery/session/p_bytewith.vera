public fn go(@Int -> @Int)
  requires(true)
  ensures(true)
  effects(pure)
{
  handle[State<Byte>](@Byte = 5) {
    get(@Unit) -> { resume(@Byte.0) },
    put(@Byte) -> { resume(()) } with @Byte = 77
  } in {
    put(1);
    byte_to_int(get(()))
  }
}

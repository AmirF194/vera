public fn go(@Unit -> @Int)
  requires(true)
  ensures(true)
  effects(pure)
{
  handle[State<{ @Nat | @Nat.0 < 10 }>](@Nat = 0) {
    get(@Unit) -> { resume(@Nat.0) },
    put(@Nat) -> { resume(()) }
  } in {
    nat_to_int(get(()))
  }
}
